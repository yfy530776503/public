spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
management:
  security:
    enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
ssso:
  user: 李四不和张三
  age: 1314
